# HORD 官网宣传截图采集与交付指南

> 本文件是 HORD 官网截图资产的唯一正式指南。
> 正式项目：`HORD-web`
> 截图目录：`screens/`
> 整理脚本：`scripts/organize_screenshots.py`
> 本指南只规范截图采集、整理和验收，不修改扩展或 Hub 运行时代码。

## 1. 使用范围与基本原则

这份指南解决的是一个具体问题：把真实产品状态稳定地转换成官网可用的宣传图片。

它不追求一次性拍摄所有可能界面，而是先保证以下四件事：

1. 截图状态可重复：不同日期重新截取时，内容和主题仍可比较。
2. 截图用途明确：首屏主图、功能卡片和完整画廊不混用。
3. 文件路径可追溯：每个代号只对应一个官网目标文件。
4. 交付结果可验收：缺图、断链、敏感信息和明显 UI 错误不能进入官网。

不要把官网截图目录当作临时素材堆放区。未进入清单、未经过验收的图片，不得直接放进 `screens/`。

## 2. 项目路径与工作目录

所有命令都应在 HORD-web 项目根目录执行：

```text
/Users/andyhu/Library/CloudStorage/OneDrive-个人/VibeCoding Work Space/HORD-web
```

目录职责：

| 路径 | 用途 |
| :--- | :--- |
| `SCREENSHOT_GUIDE.md` | 本文件，唯一正式指南 |
| `screens/` | 官网实际引用的交付图片 |
| `scripts/organize_screenshots.py` | 将 staging 图片复制到目标路径 |
| `hord_staging/` | 临时输入目录，不应提交到 Git |

macOS 推荐使用本地工作目录，避免把高频生成和反复修改直接放在 OneDrive：

```text
/Users/andyhu/Agent-Workspace/HORD-screenshot-staging/
```

Windows 可使用：

```text
%LOCALAPPDATA%\\Agent-Workspace\\HORD-screenshot-staging\\
```

旧的 OneDrive 根目录版本已删除。后续只维护 HORD-web 项目中的本文件，不再创建第二份同名指南。

## 3. 宣传素材的层级

官网不是功能清单。16 组截图可以作为完整画廊，但不应全部放在首屏或同等权重展示。

### 3.1 首屏和核心功能卡片

第一批优先准备以下 5 组：

1. 划词翻译/释义弹窗：用户最容易理解的核心入口。
2. 单词本总览：展示长期学习和数据反馈。
3. 艾宾浩斯复习：展示产品的持续使用价值。
4. YouTube 伴侣：展示真实场景中的差异化能力。
5. 阅读器或 HORD Hub：展示产品不是单一查词插件。

### 3.2 完整功能画廊

其余功能进入画廊或专题页，包括 Popup 菜单、AI 解析、同步、游戏和手机端。

如果一张图无法在缩小后立即说明功能，就不要把它当作首屏主图；它可以保留为功能证明，但不应承担首屏转化任务。

## 4. 截图采集标准

### 4.1 测试数据

- 使用固定的演示数据，不使用真实账号、真实邮箱、真实 API Key 或个人学习记录。
- 同一功能的日间/夜间截图必须使用完全相同的数据和操作状态。
- 每次截图前确认页面已经完成加载，不保留骨架屏、加载动画、空数据占位或错误提示。
- 如果页面依赖网络接口，先确认接口返回稳定；不稳定的动态内容不要作为宣传主图。

### 4.2 视口与裁剪

不要强制所有界面都使用 `1920×1080`。截图尺寸应服从产品形态和官网实际容器：

| 类型 | 建议采集方式 | 交付要求 |
| :--- | :--- | :--- |
| 桌面页面 | 固定一个视口，例如 `1440×900` 或 `1920×1080` | 同一组明暗主题必须一致；最终按官网容器裁剪 |
| 局部弹窗 | 以弹窗为主体，保留少量网页上下文 | 弹窗文字在官网展示尺寸下仍可读 |
| Popup 菜单 | 约 `360×540` 的竖向区域 | 不把整张桌面截图当作 Popup 素材 |
| YouTube 伴侣 | 保留必要的视频和字幕上下文 | 使用固定视频、固定时间点和固定字幕状态 |
| 手机端 | 使用真实设备或等比例设备模拟 | 不混入浏览器开发者工具和桌面 UI |

建议保留一份清晰的 PNG 原始图；官网展示图可以另行导出 WebP/JPEG，但不能用低质量压缩图替代原始留档。

新生成的官网展示图片建议控制在单张 `3 MB` 以内；确实需要更高分辨率的主图，硬上限建议为 `8 MB`。

### 4.3 画面清理

截图中不得出现：

- 个人姓名、头像、邮箱、账号 ID、登录二维码、支付信息；
- API Key、访问令牌、内部域名、调试日志和本地文件路径；
- 浏览器通知、无关标签页、书签栏、桌面消息和系统弹窗；
- 未授权的商业视频、书籍封面或其他可能引发版权争议的内容；
- 与当前功能无关的旧 UI、错误状态或开发占位内容。

YouTube 截图不得使用“任意英文视频”。应提前指定可公开使用的视频、URL、时间点和字幕状态，并在交付记录中保留这些信息。

## 5. 当前官网资产清单

下面的目标路径是现有官网代码和脚本使用的兼容路径。`manange单词本` 虽然存在拼写问题，但已经被网页引用；本轮不直接改名，改名必须单独进行全局迁移。

| 编号 | 功能 | 必需变体 | 官网目标路径 |
| :--- | :--- | :--- | :--- |
| 1 | 单词本总览 | `day` / `night` | `screens/manange单词本/manange单词本日间1.png` / `manange单词本夜间1.png` |
| 2 | 单词本列表 | `day` / `night` | `screens/manange单词本/manange单词本日间2.png` / `manange单词本夜间2.png` |
| 3 | 划词翻译弹窗 | `day` / `night` | `screens/content弹窗/日间模式翻译.png` / `夜间模式翻译.png` |
| 4 | 划词释义弹窗 | `day` / `night` | `screens/content弹窗/日间模式弹窗.png` / `夜间网页截图.png` |
| 5 | 弹窗展开状态 | `day` | `screens/content弹窗/日间模式弹窗2.png` |
| 6 | Popup 菜单 | `day` / `night` | `screens/popup菜单/_日间菜单栏.png` / `夜间菜单栏.png` |
| 7 | 复习进度总览 | `day` / `night` | `screens/test艾宾浩斯/艾宾浩斯日间模式0.png` / `艾宾浩斯0.png` |
| 8 | 复习任务面板 | `day` / `night` | `screens/test艾宾浩斯/艾宾浩斯日间模式2.png` / `艾宾浩斯2.png` |
| 9 | 复习行动状态 | `day` / `night` | `screens/test艾宾浩斯/艾宾浩斯日间模式4.png` / `艾宾浩斯4.png` |
| 10 | 复习统计趋势 | `day` / `night` | `screens/test艾宾浩斯/艾宾浩斯日间模式7.png` / `艾宾浩斯7.png` |
| 11 | YouTube 字幕播放 | `default` | `screens/YouTube伴侣/播放双语字幕.png` |
| 12 | 3D 电子书架 | `day` / `night` | `screens/阅读器/3D书架.png` / `3D书架_夜间.png` |
| 13 | Arcade 游戏大厅 | `default` | `screens/游戏大厅/Arcade大厅.png` |
| 14 | AI 句法透析 | `day` / `night` | `screens/AI解析/句子深度透析.png` / `句子深度透析_夜间.png` |
| 15 | HORD Hub 首页 | `default` | `screens/Hub学习中枢/Hub首页.png` |
| 16 | 跨设备云端同步 | `default` | `screens/云端同步/同步面板.png` |

这份清单共包含 **27 个目标文件**。提交前必须确认 27 个文件都存在，除非本次任务明确说明某个功能暂不交付。

## 6. staging 命名规范

截图先放入 staging 目录，再由脚本整理。staging 文件只使用以下基础名，扩展名允许 `.png`、`.jpg`、`.jpeg`：

| 基础名 | 对应功能 |
| :--- | :--- |
| `1_day` / `1_night` | 单词本总览 |
| `2_day` / `2_night` | 单词本列表 |
| `3_day` / `3_night` | 划词翻译弹窗 |
| `4_day` / `4_night` | 划词释义弹窗 |
| `5` | 弹窗展开状态 |
| `6_day` / `6_night` | Popup 菜单 |
| `7_day` / `7_night` | 复习进度总览 |
| `8_day` / `8_night` | 复习任务面板 |
| `9_day` / `9_night` | 复习行动状态 |
| `10_day` / `10_night` | 复习统计趋势 |
| `11` | YouTube 字幕播放 |
| `12_day` / `12_night` | 3D 电子书架 |
| `13` | Arcade 游戏大厅 |
| `14_day` / `14_night` | AI 句法透析 |
| `15` | HORD Hub 首页 |
| `16` | 跨设备云端同步 |

例如：

```text
HORD-screenshot-staging/
├── 1_day.png
├── 1_night.png
├── 3_day.png
├── 3_night.png
├── 11.png
└── 16.png
```

不在清单中的文件不会自动进入官网。不要把原始截图、调试截图和宣传成品混放在 staging 目录。

## 7. 当前整理脚本的真实行为

当前脚本位于 `scripts/organize_screenshots.py`，执行逻辑如下：

- 以脚本所在目录的上一级作为 HORD-web 项目根目录；
- 按固定映射寻找 staging 文件；
- 如果安装了 Pillow，会把 JPG/JPEG 转换成 PNG；PNG 通常直接复制；
- 自动创建目标子目录；
- 缺少文件时打印提示，但当前脚本仍可能以成功状态结束；
- 目标文件存在时可能直接覆盖；
- 当前脚本没有尺寸、色彩、隐私内容和文件大小校验。

因此，脚本目前是“路径整理工具”，不是完整的发布校验工具。执行前必须人工完成以下保护动作：

1. 确认 staging 目录中只有本次交付文件。
2. 确认目标文件的既有修改已经被人类知晓或备份。
3. 运行前检查 `git status`，运行后检查新增/修改文件列表。
4. 根据终端输出确认 27 个目标文件全部匹配。
5. 打开官网画廊和首页，确认没有 404、错图或明显裁剪错误。

脚本后续可以单独增强，但以下能力目前尚未实现，不能在本指南中假设它们已经存在：

- `--dry-run` 预览模式；
- `--strict` 缺图即失败；
- 默认禁止覆盖和覆盖前哈希确认；
- 图片尺寸、大小、色彩模式和可读性校验；
- 自动生成资产清单或校验报告。

## 8. 标准执行流程

### 阶段 A：准备

1. 确认本次要更新的编号和主题。
2. 准备固定演示数据和固定页面状态。
3. 确认没有真实账号信息、令牌和未授权内容。
4. 在本地 staging 目录中准备空的交付空间。

### 阶段 B：采集

1. 先采集 5 组核心宣传素材。
2. 再补齐完整画廊的其他素材。
3. 明暗主题成对采集，保持数据和布局一致。
4. 每次采集后立即按 staging 基础名保存，不要用时间戳作为最终名。

### 阶段 C：人工验收

逐张检查：

- 功能是否一眼可见；
- 文字在官网缩小尺寸下是否可读；
- 是否出现加载、错误、空数据或错位；
- 是否混入敏感信息；
- 是否与对应主题、功能和路径匹配。

### 阶段 D：整理

在 HORD-web 根目录执行：

```bash
python3 scripts/organize_screenshots.py "/Users/andyhu/Agent-Workspace/HORD-screenshot-staging"
```

Windows 示例：

```powershell
python scripts/organize_screenshots.py "$env:LOCALAPPDATA\Agent-Workspace\HORD-screenshot-staging"
```

### 阶段 E：官网验收

1. 检查 `screens/` 下目标文件是否齐全。
2. 打开 `all_gallery.html`，检查画廊图片和标题。
3. 打开 `index.html`，检查首页实际使用的图片。
4. 检查浏览器控制台是否有 404 或图片加载错误。
5. 确认图片没有把产品主体裁掉，也没有留下过多无效空白。

只有阶段 E 通过，截图才算完成交付。

## 9. 电脑控制自动截图：第二阶段方案

未来可以通过电脑控制真实浏览器和插件环境执行截图，尤其适合以下场景：

- Chrome 扩展 Popup 菜单；
- 网页中的划词和双击查词弹窗；
- YouTube 字幕伴侣；
- 需要真实浏览器上下文的 Hub、阅读器和复习流程。

建议的自动化流程是：

1. 打开固定测试页面或固定视频。
2. 准备固定演示数据和主题。
3. 通过鼠标/键盘触发目标状态。
4. 等待页面完成加载，并检查目标元素是否出现。
5. 截图保存为 staging 基础名。
6. 如果状态未出现、页面超时或登录失效，停止并报告，不生成假成功文件。
7. 自动化完成后仍由人工检查图片内容。

电脑控制不能保证以下状态始终稳定：登录会话、网络返回、动态弹窗时机、浏览器权限、第三方页面变化和视频字幕加载。因此它应当是采集执行器，不是质量判断器。

本阶段不执行电脑控制截图，也不改变现有脚本。

## 10. 新增功能或改名时的规则

新增宣传素材时，先更新本文件的资产清单，再决定 staging 代号和目标路径，最后才修改整理脚本。

任何路径改名都必须先搜索官网代码、画廊页面、Promo 页面和脚本引用。特别是 `manange单词本`，不能只改目录名而不做全局迁移。

截图路径、脚本映射和官网引用三者必须保持一致。

[Agent: Codex-GPT-5] [时间戳: 2026-07-16 01:32:07]
